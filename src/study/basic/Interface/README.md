## 💳 Java 인터페이스(Interface) 실습
인터페이스를 활용하여 서로 다른 결제 수단들에 공통된 동작 규격을 부여하고, 다중 구현을 통해 기능을 확장하는 방법을 실습

### 1. 클래스 다이어그램
<img width="534" height="464" alt="Image" src="https://github.com/user-attachments/assets/6151c8f7-59f4-4184-8d31-48293d75f688" />

### 2. 핵심 설계 구조
- **표준 규격 (PaymentProcessor)**: 모든 결제 수단이 반드시 갖춰야 할 필수 기능(validate, processPayment, printReceipt)을 정의 
- **기능 확장 (PointEarnable)**: 포인트 적립이라는 별도의 기능을 인터페이스로 분리하여, 필요한 클래스에서 선택적으로 조합할 수 있게 설계
- **유연한 결제 처리**: CreditCard와 SimplePay는 내부 로직(카드 번호 검증 vs 잔액 확인)이 완전히 다르지만, 동일한 인터페이스로 묶여 있어 일관된 방식으로 제어됨

### 3. 주요 학습 포인트
- **다중 구현**: 인터페이스는 다중 구현이 가능함. CreditCardProcessor 클래스에 두개의 인터페이스를 동시에 구현하여, 객체가 다양한 역할을 수행할 수 있도록 확장
- **디폴트 메서드**: default 메서드는 인터페이스를 구현하는 클래스에서 선택적으로 오버라이딩할 수 있다.
- **객체 간 결합도 완화**: 결제 시스템(PaymentTest)은 실제 결제가 카드로 되는지 간편결제로 되는지 알 필요 없이, 오직 PaymentProcessor라는 규격에만 의존하여 동작하므로 유지보수가 간편함
- **인터페이스 기반의 타입 통합**: 성격이 다른 결제 클래스들을 하나의 배열(PaymentProcessor[])로 관리하여, 결제 검증부터 영수증 출력까지의 과정을 한 번에 처리할 수 있다.